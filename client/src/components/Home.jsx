import React from 'react';
import Carousel from './Carousel'; // Importar el componente de carrusel

const Home = () => {
    return (
        <div className="carousel-container"> {/* Aplicar la clase para centrar */}
            <Carousel />
        </div>
    );
};

export default Home;
